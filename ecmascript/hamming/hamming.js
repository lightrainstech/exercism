class Hamming {
  compute(a, b) {


    if( a === b ) {
      return 0
    }
    aArr = new Array(...a)

    console.log(`hello`);


    for (var i = 0; i < aArr.length; i++) {
      console.log(aArr[i]);
    }
  }
}

export default Hamming
